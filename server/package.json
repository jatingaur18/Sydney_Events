{
  "name": "eventbrite-scraper-api",
  "version": "1.0.0",
  "description": "API for scraping Eventbrite events",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install-chrome": "node -e \"const puppeteer = require('puppeteer'); (async () => { console.log('Installing Chrome...'); })();\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "express-rate-limit": "^6.7.0",
    "puppeteer": "^22.15.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}